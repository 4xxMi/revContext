import { Surface, Text } from "react-native-paper";
import styles from "../config/styles";

export default function HomeScreen() {
    return (
      <Surface style={styles.container}>
        <Text> ola Vlad Dracula Tepes </Text>
      </Surface>
    )
  }